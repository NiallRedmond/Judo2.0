<% if admin? %>

<%= form_for(@judo_class) do |f| %>
  <% if @judo_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@judo_class.errors.count, "error") %> prohibited this judo_class from being saved:</h2>

      <ul>
      <% @judo_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :belt_level %><br>
  <%= f.select :belt_level, ['White', 'Yellow', 'Orange', 'Green', 'Blue', 'Brown', 'Black'] %>
  </div>
  <div class="field">
    <%= f.label :class_name %><br>
    <%= f.text_field :class_name %>
  </div>
  <div class="field">
    <%= f.label :day %><br>
    <%= f.text_field :day %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.time_select :time %>
  </div>
  <div class="field">
    <%= f.label :teacher_name %><br>
    <%= f.text_field :teacher_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<% else%>
<h4><i><font color=red>Sorry, you have to be an admin create a new class.</font></i></h4>
<% end %>